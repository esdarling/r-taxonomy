---
title: "save-descriptions"
author: "Emily Darling"
date: '2019-03-06'
output: word_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, message = FALSE)
?opts_chunk
library(here)
#source(here("0-google-docs-import-data.R"))
source(here("0-csv-import-data.R"))
source(here("1-wrangle-gather-species.R"))

```

```{r wrap-hook}
library(knitr)
hook_output = knit_hooks$get('output')
knit_hooks$set(output = function(x, options) {
  # this hook is used only when the linewidth option is not NULL
  if (!is.null(n <- options$linewidth)) {
    x = knitr:::split_lines(x)
    # any lines wider than n should be wrapped
    if (any(nchar(x) > n)) x = strwrap(x, width = n)
    x = paste(x, collapse = '\n')
  }
  hook_output(x, options)
})
```

```{r collapse and setup for knit, linewidth=60}
df <- spp.collapse

for (i in 1:nrow(df)) {
  print(df[i, "species"])
  print(df[i, "description"])
}

```
